#include "main.h"
/**
  *_abs - always return a positive number
  *@n: any integer
  *Return: returns the absolute value of any integer
  */
int _abs(int n)
{
	if (n < 0)
	{
		return (n * -1);
	}
		return (n);
}
